name: build test image

on:
  push:
    paths:
      - '.github/workflows/build-test-image.yml'
      - 'env/**'
    branches:
      - '**'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: LDMX-Software/pflib-env

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        hosttype:
          - zcu
          - bittware-host
    steps:
    - uses: docker/setup-qemu-action@v4
    - uses: docker/setup-buildx-action@v4
    - uses: docker/login-action@v4
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - id: date
      run: echo "date=$(date +'%Y.%m.%d')" >> "${GITHUB_OUTPUT}"
    - id: meta
      uses: docker/metadata-action@v6
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=sha,prefix=${{ matrix.hosttype }}-sha-
          type=raw,value=${{ matrix.hosttype }}-${{ steps.date.outputs.date }},enable=${{ format('refs/heads/{0}', github.event.repository.default_branch) == github.ref }}
          type=raw,value=${{ matrix.hosttype }}-latest,enable=${{ format('refs/heads/{0}', github.event.repository.default_branch) == github.ref }}
    - uses: actions/checkout@v7
    - uses: docker/build-push-action@v7
      with:
        context: env/
        file: env/Containerfile.${{matrix.hosttype}}
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
