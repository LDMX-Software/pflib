name: zcu build

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v5
      - run: |
          docker run --platform linux/arm64 ghcr.io/ldmx-software/pflib-env:sha-8244ecf 'cmake -B build -S . && cmake --build build'
      - uses: actions/upload-artifact@v5
        with:
          path: build
