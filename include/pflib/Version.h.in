/**
 * pflib Version file copying parameters in from the CMake build configuration
 */

#pragma once


#define PFLIB_VERSION "${pflib_vers}"
#define GIT_DESCRIBE "${git_describe}"

/**
 * print a two-line version message to stdout
 */
void print_version();
