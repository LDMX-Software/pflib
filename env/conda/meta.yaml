package:
  name: pflib
  version: v3.9.0 #{{ environ.get('GIT_DESCRIBE_TAG') }}
source:
  path: ../..
build:
  exclude:
    - build/**
    - .git/**
requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - git
    - cmake
    - make
    - python
    - boost
    - rogue=v6.7.0
    - nlohmann_json
    - yaml-cpp
  run:
    - python
    - boost
    - rogue=v6.7.0
    - nlohmann_json
    - yaml-cpp
